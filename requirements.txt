# Pinned to the versions used to produce the results reported in the paper.
lightgbm==4.7.0
xgboost==3.4.1

# Versions below were not recorded during the original run. Replace the
# lower bounds with exact pins by running the snippet in README.md
# ("Recording exact versions") inside the notebook environment.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
